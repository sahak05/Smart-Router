# Smart-Router
A desktop Smart Router for IoT with device inventory, packet capture, Allow-list Firewall and IPS. This desktop router will expose who is connected, what traffic flows, an applies a minimal-friction allow-list plus an anomaly-based throttle to contain misbehavior.
